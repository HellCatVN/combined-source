// @ts-nocheck

import logo from '@assets/logo/logo.png';
import { customConfig } from '@bm/configs';

export const config = {
  tabViews: false,
  signUp: false,
  logo: logo,
  logo_collapsed: logo,
  ...customConfig,
};
